name: Build Roblox 2016
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest # Changed from windows-2019
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2
        
      - name: Install v140 Toolset (VS2015)
        run: |
          choco install visualstudio2019-workload-vctools --package-parameters "--add Microsoft.VisualStudio.Component.VC.140" -y


      - name: Build
        run: |
          # Add your build commands here
          echo "Starting build..."
